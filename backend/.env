# Backend .env - generated defaults, replace placeholders before production

# Flask
FLASK_ENV=development
PORT=5000
SECRET_KEY=dev-secret-key-change-in-production

# Database (MongoDB)
MONGO_URI=mongodb://localhost:27017/learning_platform

# JWT
JWT_SECRET_KEY=jwt-secret-change-in-production
JWT_ACCESS_TOKEN_EXPIRES=1    # hours
JWT_REFRESH_TOKEN_EXPIRES=30  # days

# External APIs
GEMINI_API_KEY=AIzaSyCvYdcR3fohlroYxxcS1u9jXfMyybm8zl4
OPENAI_API_KEY=your_openai_api_key_here

# Celery / Broker (configure if using external broker)
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# Rate limiting
RATELIMIT_DEFAULT=100 per hour

# File upload
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# CORS (comma-separated origins allowed)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Other
# Add other env variables below as required by the application
